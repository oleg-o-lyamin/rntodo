import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  root: {
    width: 20,
    height: 20,
    borderRadius: 30,
    borderWidth: 1,
    borderColor: '#000',
  },
  inner: {
    height: '100%',
    width: '100%',
    backgroundColor: '#000',
    borderRadius: 30,
  },
});
